# Campus Atlas — handoff context (session 6, continued from session 5)

## SESSION 6 — read this first: what's done, what's next, and the two half-built features

Session 6 was mostly UX and feature work on top of the 668-programme catalogue, plus
three product features the user asked for in order. **Two of those features are
infrastructure-complete but data-thin on purpose** — the mechanism is built and one
correct, sourced example is in place; populating them is a per-school reading task that
should NOT be done by guessing. The user has explicitly, repeatedly said: only publish a
figure read from the school's/authority's own page, link it, and leave it blank otherwise.
Hold that line — it is the whole product's credibility.

### What shipped this session (all committed + pushed)
1. **Scholarship precision** (commit `dfd1f9d`): every funding card now filters by the
   user's citizenship (ineligible schemes are hidden, not shown "to check"), shows a
   real amount computed against the programme's own fee/length (`estimateAward`), and a
   colour-coded odds chip whose evidence is a fact the awarding body publishes. Structured
   fields `fit` / `award` / `odds` on GOV_SCHOLARSHIPS + GLOBAL_SCHOLARSHIPS, and `award`
   / `odds` on 25 SCHOOL_SCHOLARSHIPS.
2. **Terms/Privacy page + source-linking** (commit `516c62f`): new Legal page; every
   figure carries a `sourceLine()` linking the page it came from; two real privacy leaks
   fixed (Leaflet + Nominatim were called on page load — now on demand only).
3. **Test-centre locator rebuilt** (commit `27cde71`): the OSM "nearby centres" search
   was deleted — OSM genuinely holds no Pearson/Prometric POIs (verified via Overpass:
   zero within 60 km of Brussels). Now geocodes the place, shows it on a map, and the
   buttons are location-anchored maps searches + the official booking page.
4. **GMAT/GRE prep hub** (commit `c59453f`): full section at the bottom of the checklist
   page, jump button in each programme's card. All test-format facts sourced from GMAC/ETS.
5. **NL + FR site** at `/nl/` and `/fr/` (commits `83dd141`, `656b1f6`, `998fa69`):
   generated by `build-i18n.js` from `index.html` + `i18n/{nl,fr}.json`. **Run
   `node build-i18n.js` after ANY edit to index.html** or the translated pages go stale.
   The switcher is top-right on desktop, bottom of the hamburger on mobile. Replacement is
   context-aware and guarded against catalogue values — see the big comment in the script.
6. **"Your situation" panel removed** (commit `7bab5f6`): it asked three questions and
   changed nothing for ~95% of users. Replaced by a static "Who this programme is built
   for" block in the modal, plus a new **`prereq`** field for hard prerequisites (a prior
   master's, a required first degree, a portfolio). 21 programmes have one, all verified —
   the trigger case was Vlerick: *you need a master's already to do a Vlerick master's if
   you studied in Belgium*, quoted from their admissions page.
7. **Alumni outcomes** (commit this session): `PROGRAMME_OUTCOMES` table + a "Where
   graduates end up" modal block. **14 entries now, all verified against the school's
   own employment report (see session 7 note below).**

### SESSION 7 — Dual/joint degrees added to the catalogue (668 → 673)
User asked for dual programmes ("MBA and a Master in Science"). Added five two-year
joint degrees, each linked to the school's own page: **MIT LGO (691)** MBA+MS
Engineering, **Kellogg MMM (692)** MBA+MS Design Innovation, **Wharton Lauder (693)**
MBA+MA International Studies, **Harvard MS/MBA Engineering Sciences (694)**, **Berkeley
MBA/MEng (695)**. Key decisions: (1) **No `extRank`** — joint programmes carry no
standalone QS/FT number and the ranking-hygiene rule forbids borrowing the parent
MBA's, so they sort by `500 + rank` (rank 40–44). (2) **Tuition** stored in EUR =
official per-year figure × programme years ÷ 1.08 (US rate) — MIT $91,892/yr, Kellogg
$86,370/yr, Wharton $92,820/yr tuition+fees; Harvard & Berkeley are two-year duals
paying the parent MBA tuition, so they reuse the catalogue's verified HBS/Haas total
(EUR 160000). (3) **Skipped on the tuition rule:** Yale MBA/MEM, Stanford duals, Booth
joint MPCS — all 3-year or MBA+separate-MS-fee structures with no single official
figure to store (blank beats wrong). Remaining clean 2-year-MBA-tuition duals for a
future pass are thin; the well-known flagships are now in. All schema-validated
(fields against FIELDS, no dup id/school+program), URLs 200-checked, NL/FR rebuilt.

### SESSION 7 — Feature A populated to 9 entries (all from the school's own report)
Added 8 Full-Time MBA outcome entries, each figure read from the school's own
employment report (MBA CSEA standard = 3 months after graduation) and linked:
Wharton (197), MIT Sloan (199), Columbia (201), Kellogg (202, Class of 2025 —
its live page had rolled to '25), Booth (203), Berkeley Haas (204), Yale SOM (448),
Duke Fuqua (449). Method that worked: **the school's own employment-report PDF/page
is the primary source** — curl the PDF with a browser UA and parse with pypdf (needs
`cryptography` installed for AES-encrypted PDFs like Haas's), or curl the HTML report
page (som.yale.edu, kellogg career-outcomes both curl-fetchable; Booth via WebFetch).
`.edu` career *landing* pages 403 both WebFetch and curl (Ross, Columbia HTML) — but
their **report PDFs** on the same domain serve fine, so link the PDF. All 8 source
URLs 200-checked. Render tweak: dropped the hardcoded "employed" word from
`outcomesHTML` so each entry's `employedWindow` carries its own verb ("received a job
offer…", "accepted…", "employed…") — accurate for offer-rate vs acceptance-rate figures.
Second batch added three European flagships from their own employment pages: HEC
Paris (441, Class of 2025 — page gives placement rate + sectors but no salary, so
salary left blank), INSEAD (444, Dec-2024 + Jul-2025 classes), IESE (447, Class of
2025 — placement + sectors, no salary). Note `intheknow.insead.edu` 403s curl but
WebFetch reaches it and it works in a browser (documented INSEAD Cloudflare block).
Third batch: added Cambridge Judge (443, Class of 2024/25) from jbs.cam.ac.uk — note
Cambridge reports on a **4-month** window and an **average** (not median) base salary,
both kept verbatim; its sector split is a chart image so no percentages shown.
**Deliberately skipped, with reason:** Oxford Saïd (446) — sbs.ox.ac.uk sits behind a
Cloudflare bot-challenge that curl/WebFetch/the in-app browser can't clear (do not
bypass); its employment-report PDF (sbs.ox.ac.uk/.../mba-employment-report-25.pdf)
opens fine in a human browser, so read it there next time. IE (445) — its career-report
PDF's headline "95% within 3 months" is a school-wide "master's graduates" claim, not
IMBA-specific, and it carries no parseable salary, so it fails the degree-match rule.
Still unpopulated (same method): LBS MBA (442, PDF on assets.london.edu), Oxford (446,
human browser), IE (445, needs an IMBA-specific figure), and the MiM entries.

### ⚠️ HALF-BUILT FEATURE A — Alumni outcomes (user step 2 of 4)
`PROGRAMME_OUTCOMES` in index.html is keyed by programme id. Fields (all optional):
`employedRate`, `employedWindow`, `salary` (keep the school's own currency string),
`sectors[]`, `employers[]`, `alumniCount`, `alumniCountries`, `note`, `classYear`,
`source {url,label}`. `outcomesHTML(p)` renders it, with a "not verified yet" fallback.
**To populate:** the reliable sources are (a) each school's **employment report** (search
`"<school> masters in management employment report"` — they're usually PDFs, parse with
pypdf as in earlier sessions), and (b) the **FT Masters in Management / MBA tables**, which
give a standardised "% employed at 3 months" + weighted salary per ranked school (FT
method already documented lower in this file). Do NOT guess career-page URLs one at a
time — that wasted real time this session; the pages are JS-heavy and hard to find. Go via
the FT report or a site search. LBS's numbers came off london.edu's MiM page directly.

### ⚠️ NOT STARTED — FEATURE B — Fairs & events (user step 3 of 4)
The user wants, **for European business schools only** as a first cut: which recruitment
fairs / info sessions / open days each programme will be at, and — if one is near the
user — a link to register. Nothing is built yet. Suggested shape, matching the rest of the
app: a `PROGRAMME_EVENTS` (or school-level `SCHOOL_EVENTS`) table with
`{name, city, country, date, url, kind}`, filtered against the user's location using the
existing `geocodeLocation()` + `haversineKm()` already in the locator code. The big fairs
to seed from (all publish their own schedules): **QS World MBA/Masters Tours, Access
Masters, Access MBA, the FT/Poets&Quants events, and each school's own "meet us"/open-day
page** (Vlerick, HEC, ESSEC, ESCP, Bocconi, IE, IESE, Esade, LBS, RSM all have one). Same
sourcing rule: real dated events with a registration URL, or nothing. Note events are
time-sensitive and will need a freshness note like the scholarships' "last checked".

### STANDING TASK — more programmes + more granular scholarships (user step 4, ongoing)
Still the biggest open item. Catalogue is 668. Gaps unchanged from session 5's list
(below): France/Italy/Spain thin outside business schools, US/Asia top-50 thin, Zhejiang
still 0 (its English catalogue is parsed in session 5's log, just needs writing up),
fashion programmes missing. Scholarships: `SCHOOL_SCHOLARSHIPS` is ~115 of ~250 schools;
`GOV_SCHOLARSHIPS`/`HOME_REGION_FUNDING`/`GLOBAL_SCHOLARSHIPS` still need the finer
granularity the user asked for (per-programme awards, not just nationality-tied). The
build tooling from session 5 (bulk curl of official pages; QS JSON via the in-app browser)
is unchanged and is how to keep going — see the two breakthroughs section below.

### House rules confirmed this session (do not violate)
- **Every figure links to its source and shows "last checked".** No estimates presented as
  facts. Blank beats wrong.
- **After editing index.html, run `node build-i18n.js`** and eyeball the "stale keys" report.
- **Never let a dictionary key collide with catalogue data** — the guard in build-i18n.js
  handles FIELDS/REGIONS/openFields, but if you add new data-carrying string constants,
  extend `protectedValues()`.
- **Verify data integrity after i18n builds**: `node -e` parse of nl/ and fr/ must give 668
  programmes and badFields=0 (there's a one-liner in this session's log).

---

## SESSION 5 — the two tooling breakthroughs, read these first

Session 4's method (pull ranking table → diff → apply ranks → add schools) still
holds. Session 5 replaced the *plumbing* underneath it, and that is what makes
volume possible.

### 1. Bulk-fetch official pages with curl, don't WebFetch them one at a time
`scratchpad/fetch.sh` + `extract.py` fetch 12 URLs in parallel and print every
fee / duration / deadline line. **This is 20× faster than WebFetch per page and
it reads the school's own figure**, which is the standard the user set explicitly:
*"always refer to the official websites for any information regarding tuition or
timelines. Always make sure you have that official link in there."*

Where it works (fee is in the server-rendered HTML): **UCL, Imperial, KU Leuven,
Ghent, ULB, UCLouvain, VUB, Antwerp Management School, Vlerick, Sheffield,
Glasgow, Nottingham, Warwick**.
Where it does **not** (fee is injected by JS): **Oxford, Cambridge, Edinburgh,
KCL, Manchester, Bristol, Monash, UNSW, UQ**. For those, use the browser.

Two tricks that saved a lot of guessing:
- **`/sitemap.xml`** is the fastest way to enumerate a school's real programme
  URLs (found all six Vlerick masters this way after the nav-scrape failed).
- Fetching an index page and grepping `href` gives the full slug list — UCL's
  A–Z yielded all 550 taught degrees in one request.

### 2. topuniversities.com is directly readable through the in-app browser
`xuanxiao.org` no longer mirrors the subject tables, and curl gets a Cloudflare
403. But **the in-app browser clears Cloudflare**, and from a page on that origin
`fetch()` reaches QS's own JSON endpoint:

```
/rankings/endpoint?nid=<NID>&page=<0-based>&items_per_page=100&tab=indicators
   header required: X-Requested-With: XMLHttpRequest      ← without it you get HTML
```
`<NID>` comes from `"currentPath":"node\/<NID>"` in the ranking page's HTML.
A helper (`window.__qsGet(slug, pages)`) is in the session log. This is the
**ranking body's own site**, so it outranks every mirror when they disagree —
which is how UCL's Computer Science label was corrected from #22 to #20.

Tables pulled and saved to `scratchpad/qs2026.md`: computer science, mathematics,
electrical, mechanical, civil, chemical, economics, accounting & finance,
business & management, law, politics, physics, chemistry, biological sciences,
environmental sciences, statistics & OR, architecture, education, materials,
medicine, psychology, communication, development studies, earth & marine,
pharmacy, agriculture.

### ⚠️ QS World 2027 is out — the catalogue is still on 2026
The World table now live is **2027** (`nid=4153156`); the *subject* tables are
still 2026 (published 25 March 2026), so a mixed year is correct, not a bug.
European 2027 positions are saved in `scratchpad/qsworld2027.txt`. Spot-checked
schools were unchanged (KU Leuven 59, Ghent 150, UCLouvain 196), so new entries
added in session 5 use `(2027)` while older ones still say `(2026)`.
**Unfinished: sweep the existing `QS World University Rankings #N (2026)` labels
against that file and refresh them.** Do not bump a year without checking the
number.

## Session 5 — what changed

- **PROGRAMS 551 → 668.** UCL +30 (was 4 entries at a QS #9 school with 550
  taught degrees), KU Leuven +27, TUM +21, ULB/Solvay +14, Imperial +12,
  Antwerp Management School +7, Vlerick +5.
- **`SCHOOL_SCHOLARSHIPS` 103 → 115**, weighted to the six focus countries.

### ⚠️ The link-quality problem — the biggest open defect
**318 of 656 entries linked to a homepage or a department landing page rather
than the programme's own page**, which is exactly what the user ruled out
("always make sure you have that official link in there"). Imperial's eight were
fixed this session, and four of those turned out to name courses Imperial no
longer offers under that title — so a wrong link was hiding wrong content.
Expect the same elsewhere. A ready-made audit script pair
(`scratchpad/check.sh` + `allurls.txt`) will curl every catalogue URL and print
its status code; **run it against a slice at a time, not all 700 at once.**
Note the heuristic used to find these has false positives: `uclouvain.be/en-prog-2026-<code>`
and `mba.nus.edu.sg` are one path segment deep but *are* the programme page.
- **Six new `FIELDS`**: Mathematics, Architecture, Psychology, Media &
  Communication, Agriculture & Food, Humanities — each with `RANK_SUBJECT_HINTS`
  entries so a rank in those QS tables counts as relevant to the query. The
  catalogue previously had no tag at all for a maths, architecture or psychology
  degree.
- **Academic background rebuilt (user request).** The four-way select became
  `BACKGROUND_OPTIONS`: 7 optgroups, 29 subjects. Each carries a **conservative**
  roll-up to the four legacy broad labels, so the 580 entries tagged only
  "STEM & Engineering" still match. Psychology deliberately does *not* roll into
  STEM. Programme `openFields` may now mix broad and granular values;
  `backgroundAccepts()` handles both.
- **Stale figures corrected against the school's own page**, all found by the
  bulk fetch: UCL MSc Computer Science (€41,000 → the published £42,700), Vlerick
  MiM (€19,500 → €20,950, and off the homepage onto the programme page), Antwerp
  Management School (€24,000 → €19,900), Solvay Business Engineering (flat
  €6,000 → the FWB's €1,194 EU / ≈€5,369 non-EU). **Three of those four entries
  linked to a homepage, not a programme page — worth auditing the rest for that.**

### Belgian fee structures, as published (useful, non-obvious)
- **KU Leuven** 2026-27 tariffs per 60 ECTS: €1,181.40 / €3,093.12 / €5,613.12 /
  €9,493.92. The tariff is assigned *per programme*, and KU Leuven says its fee
  **tool** is "the only official information" — the programme pages themselves
  carry no figure. Entries use the top non-EEA tariff and say so.
- **Ghent** splits non-EEA fees by faculty: **Fee A €2,297.40** (Arts, Law,
  Psychology) vs **Fee B €7,079.40** (Sciences, Engineering, Economics, Medicine,
  Vet, Bioscience, Pharmacy, Political & Social Sciences).
- **ULB and UCLouvain** share the Wallonia-Brussels Federation scale: €1,194 full
  fee, €835 intermediate, €374 low income, €0 for grant holders, **plus a €4,175
  non-EU contribution** (ARES Circular 2026-001).
- **VUB** non-EEA is a formula: €2,200 fixed + €52/credit.
- ULB programme pages are `ulb.be/en/programme/<CODE>` (MA-INFO, MA-IRIF,
  MA-DROI, MA-PHYS, MA-CHIM, MA-MATH, MA-STAT, MA-URBA, MA-ARCH, MA-PSYC,
  MA-GEOG, MA-ETEU, MA-COMU, MA-INGE, MA-GEST, MA-ECON all verified live).

## STILL TO DO — the user's brief, unfinished
The ask was: every relevant programme at the top-50 schools globally across all
domains; comprehensive scholarships for **Belgium, France, UK, Spain, Italy,
Germany**; and application checklists tailored per programme. Delivered so far is
Belgium and UCL. Remaining, in the user's own priority order:

1. **France, Germany, Italy, Spain** — currently 38 / 16 / 13 / 18 entries and
   almost entirely business schools. Germany 16 entries covers 12 schools with no
   science or humanities breadth at all. **Germany's fee picture must be verified
   per state** — Baden-Württemberg charges non-EU €1,500/semester, Bavaria (TUM,
   LMU) introduced non-EU fees from winter 2024/25, everywhere else is a semester
   contribution only. Do not assume "Germany is free".
2. **Rest of the UK** — Oxford, Cambridge, Edinburgh, Manchester, KCL and LSE are
   all thin, and all need the browser rather than curl.
3. **US/Asia top-50** — Caltech (2 entries), Chicago (2), UPenn (2), Princeton
   (2), Northwestern (2), UNSW (2), Sydney (2), Fudan (2), ANU (1), CUHK (1),
   Monash (1), SJTU (1), Yonsei (1), **Zhejiang (0)**. Zhejiang's full
   English-taught catalogue with per-programme fees was parsed out of its 2026 PDF
   and is in this session's log — it just needs writing up.
4. **Specialist domains the user asked for by name: fashion and supply chain.**
   Supply chain got Antwerp Management School and Vlerick; still missing IFM Paris,
   Istituto Marangoni, Polimi/Bocconi fashion-and-luxury, LCF, Parsons.
5. **Scholarships** — started, nowhere near done. `SCHOOL_SCHOLARSHIPS` is 115
   of ~250 distinct schools. `GOV_SCHOLARSHIPS` and `HOME_REGION_FUNDING` were
   not touched at all, and the user specifically asked for awards tied to the
   *programme* rather than the applicant's nationality — `GLOBAL_SCHOLARSHIPS`
   is still the 11 entries session 4 left. Warning from this session: roughly
   half the scholarship URLs guessed from a plausible pattern were dead. Verify
   every one with curl before committing it.
6. **`PROGRAMME_ESSENTIALS` is 14 of 635.** This is the weakest part of the site
   relative to the brief.

## READ THIS FIRST — the method that works

The catalogue is now **551 programmes**. The single highest-value activity is not adding rows, it's this loop, per domain:

1. **Pull the published ranking table.** QS subject/business-masters tables via `xuanxiao.org/en/rankings/qs/...` (a mirror that WebFetch can read; `topuniversities.com` returns 403). The FT tables are only published as PDFs — download with WebFetch and parse with `pypdf` (the mirror sites only carry a top 10).
2. **Diff it against `PROGRAMS` with a Node script** (see the one-liners throughout this session's commits): for each ranked school, does an entry exist *for that degree*?
3. **Apply the rank to entries that already exist but have none** — this matters more than adding new rows. An entry with no `extRank` scores `500 + rank` in `prestigeScore()`, i.e. it sorts below every ranked programme. Whole domains (Law, Public Health, most MiM entries) were invisible purely because of this.
4. **Then** add the missing schools, with tuition verified from the school's own fees page.
5. **Verify every new link** before committing: `git diff | grep '^+' | grep -o 'https\?://...' | xargs -P 8 curl -o /dev/null -w "%{http_code}"`. This has caught a dead link in almost every batch, including pre-existing ones.

### Ranking hygiene — non-negotiable
- **Never write an `extRank` by guessing an id.** Grep the entry, assert on a distinctive substring of the *programme name*, then write. Two misattributions happened this session and were caught only by re-reading the output: Cambridge's Environmental Sciences #6 briefly landed on Sciences Po, and Queen's Smith nearly took a rank belonging to a different Queen's degree.
- **The rank must match the degree, not just the school.** FT MiM #72 for Queen's is their Master of International Business; the catalogue's Queen's entry is Management Analytics, so it was left unranked. Same reason Stockholm School of Economics' MiM rank is not on its MSc Finance entry.
- **Only structured `QS ... (YYYY)` / `FT ... (YYYY)` labels drive sort order** (see `prestigeScore()`). Prose like "Africa's #1 MBA" is displayed but deliberately ignored — don't "fix" that.
- **Mirrors disagree** (Imperial CS #8 vs #12, EPFL #11 vs #15). Prefer the ranking body's own summary text; where mirrors conflict, leave the existing value alone rather than churn.

## Session 4d — ranking-label completeness + a full link audit

- **Every subject table I could verify is now on the 2026 edition.** Refreshed this pass: Education (UCL #1, Harvard GSE #2, Stanford/Oxford #3, OISE #10), Earth & Marine Sciences (ETH is world #1), Engineering & Technology, Aerospace. Entries still on 2025 labels: **7**, and they should stay there — they are "Top N" bands for tables where the school does not appear in the 2026 top list I could verify (Melbourne mechanical, HKU law, Utrecht environmental) or for tables I have not pulled (Life Sciences & Medicine, Biomedical Engineering). **Bumping the year without the data is fabrication.**
- **QS publishes no standalone aerospace table** — aerospace sits inside Mechanical/Aeronautical/Manufacturing. That is where the aerospace numbers come from.
- **Relevance fix**: a subject ranking naming the searched subject now counts as a *direct* match (tier 0), not a subject-area one. MIT's "SM in Aeronautics and Astronautics" is ranked #1 in aerospace but was sorting below a #9 purely because its title says Aeronautics. Knock-on wins: finance now surfaces Oxford Saïd #2 second, AI surfaces Tsinghua #11 second.
- **Full link audit: all 729 URLs in the file, not just the diff.** 33 were dead — mostly deep programme pages from earlier sessions whose schools have restructured (Cornell Tech, Babson, DTU, ISB, Tuck, Harvard Data Science, HEC's data-science MSc, Delft aerospace, RSM analytics, UBC Sauder, Tsinghua admissions, UC3M and a dozen departmental pages). All repointed to the nearest live page on the same official domain. **Re-run this audit periodically — deep links rot fast.** The remaining non-200s are Cloudflare/403 bot blocks (ox.ac.uk, insead.edu, daad.de, chevening.org) that work in a browser.
- **Supply chain** completed to #15 (CBS, Purdue, HKUST). Note: an aggregator reported the CBS non-EU fee as $120,000, off by roughly an order of magnitude — CBS's own structure was used instead. **Aggregator fee figures need a sanity check against the school's own page.**
- **`SCHOOL_SCHOLARSHIPS` 92 → 103**, including the three Chinese universities (where CSC, not the school, is the actual mechanism), Caltech and Georgia Tech (both doctoral-funded; master's candidates should expect to self-fund), and Prague (Czech-taught degrees are free to everyone regardless of nationality).

## Session 4c — second ranking sweep, 531 → 548 programmes

Same loop as 4b, applied to the tables that were still missing or stale.

**New coverage**: Supply Chain (a fifth QS business-masters table — ESSEC #1, WU Vienna #3, RSM #4 added), Electrical Engineering (NUS #3, EPFL #10), Civil Engineering (NUS #2, Imperial #7), Law top 15 completed (Stanford #5, Berkeley #10, UCL #13), Finance ranks 16–24 (IE, Warwick, Esade, emlyon, EDHEC), Marketing ranks 10–15 (emlyon, EDHEC, WU Vienna, Manchester, St Gallen), Management #14/#29 (LSE, KCL), Business Analytics 28–38 (SMU Singapore, Edinburgh, Georgetown, Trinity), three more US MBAs (McCombs #37, Georgetown, UW Foster), and six global top-100 universities that had no entry at all: Lund, Uppsala, Kyoto, Paris-Saclay, PSL, City University of Hong Kong.

**Ranking labels refreshed to the 2026 edition**: Computer Science (11 entries), Physics & Astronomy (6), Chemistry (5), Chemical Engineering (5), Civil (6), Electrical (2). The ~20 entries still carrying 2025 labels are all "Top N" bands for tables I have not verified for 2026 (Engineering & Technology, Earth & Marine, Aerospace, Education) — **leave them alone rather than bumping the year without the data**. The 54 `FT Global MiM (2025)` labels are correct; that is the current FT edition.

**`extRank` coverage**: 24 entries at KU Leuven, Ghent, UCLouvain, LMU, Heidelberg, TU Berlin, EPFL and Wageningen had no ranking at all and were given their **QS World University Rankings 2026 institution position**. Labelled explicitly as the world ranking, which means `queryAwarePrestige()` discounts it on subject searches — so it improves place-based searches ("belgium" now leads with KU Leuven #59) without ever outranking a genuine subject rank. Entries with no ranking: 94 → 70.

**`SCHOOL_SCHOLARSHIPS` 74 → 92.** The six top US law schools were the biggest gap and behave unlike business schools: Harvard, Yale and Stanford give LLM aid on demonstrated need only with no merit track, and international students cannot access US federal loans. NYU (Hauser Global Scholarship — full tuition plus stipend) and Columbia (Bhagwati Fellowship) are the two that genuinely fund international LLM candidates.

## Session 4b — ranking sweep (QS + FT), 503 → 531 programmes

### Method now established for ranking work
For each domain: pull the published table, diff it against `PROGRAMS` with a Node script, add the missing schools with verified tuition, and — just as important — **apply the subject rank to entries that already existed but had none**, because an unranked entry sorts below everything. Sources used: QS Business Masters 2026 (Business Analytics, Management, Finance, Marketing), QS Global MBA 2026, QS subject 2026 (Computer Science, Mechanical/Aero/Manufacturing, Environmental Sciences), and the **FT Masters in Management 2025 table parsed out of the FT's own PDF** (`pypdf`; the mirror sites only publish a top 10).

### Coverage now
- **MBA** — every school in the QS 2026 Global top 15 is present, plus most of the top 50. 55 entries, ordered 1→30 cleanly.
- **Business Analytics** — QS 2026 top 20 complete (was missing 7 of the top 10, including #1 and #2).
- **Masters in Management** — FT 2025 ranks applied to 27 previously-unranked entries; six FT top-50 schools added.
- **Finance** — QS 2026 top 15 complete.
- **Marketing** — QS 2026 top 8 complete.
- **Mechanical engineering** — QS 2026 top 20 gaps filled (NUS #3, Cambridge #4, NTU #5, EPFL #13).
- **Sustainability** — QS Environmental Sciences 2026 ranks applied; Wageningen (joint world #2) added.

### Two real ranking bugs found and fixed here
1. **Ranking labels are not on a common scale.** `QS Supply Chain Management #2` sorted as a "2" in an *AI* search, putting a supply-chain degree above every AI programme. `queryAwarePrestige()` now only trusts a rank when the ranking's subject matches the query's subject (`RANK_SUBJECT_HINTS`); otherwise +200.
2. **Relevance tiers were an absolute gate.** An unranked title match outranked a world #2 subject-area match — under "sustainability" that buried Oxford #2 and Wageningen #2. A tier is now worth 40 places, not infinity. The "Related programmes" divider became a per-card note as a result, since results are no longer in strict tier blocks.

### Hero banner — settled, don't re-litigate
The dark-mode banner went through three states in one session because I over-solved it. **Final state: one image, the original daylight coloured-pencil drawing, in both colour schemes.** Dark mode changes only the overlays (wash → dark page colour, deeper vignette, flipped text halo, image dimmed to `brightness(.86)`, eyebrow forced to full-strength `--ink`). A sunset variant and then a night variant were both built and both rejected — the user's actual complaint was only ever that a `display:none` rule was deleting the image. If the banner comes up again, fix the CSS, don't regrade the art.

### Mobile input zoom — the non-obvious one
"The search button shifts right and the page slides sideways when I type" was **not** a layout bug. iOS Safari auto-zooms the whole page when a focused input has `font-size` under 16px. All text inputs and selects are now 16px below 620px. If a similar "page drifts on mobile" report appears, check input font-size first.

### Also in this pass
- **Dark-mode homepage banner restored.** A dark-scheme rule was setting `.hero-photo-layer { display:none }`, so evening visitors got no image at all. Added `assets/stanford-pencil-hero-evening.png` — the same coloured-pencil drawing graded to dusk by a luminance-driven split-tone remap (script logic is in the commit message; regenerate with Pillow + numpy if the base art ever changes). Dark mode swaps image, wash, vignette, grain blend and text halo.
- **`SCHOOL_SCHOLARSHIPS` 58 → 74**, prioritised by ranking. Includes the genuinely useful negative finding that **MIT and Stanford largely do not fund master's students** — worth keeping, it's the kind of thing applicants discover too late.

### Ranking hygiene rules learned the hard way
- **Never set an `extRank` by guessing an id.** Two misattributions happened this session (Cambridge's Environmental Sciences #6 briefly landed on Sciences Po; Queen's Smith nearly took a rank belonging to a different Queen's degree). Always grep the entry and assert on a distinctive substring of the *programme name* before writing.
- A rank must match the *degree*, not just the school. FT MiM #72 for Queen's is their Master of International Business; the catalogue's Queen's entry is Management Analytics, so it was deliberately left unranked.
- Third-party QS mirrors disagree with each other (Imperial CS #8 vs #12, EPFL #11 vs #15). Prefer the ranking body's own summary text; where mirrors conflict, leave the existing value alone.

### Domain coverage as it stands (all verified by a live search, ordering checked)
| Domain | Search returns | State |
|---|---|---|
| MBA | 1,2,3,4,5… | QS 2026 top 15 complete; most of the top 50 |
| Business Analytics | 1,2,4,7,8… | QS 2026 top 20 complete; 28–38 band added |
| Masters in Management | FT 1,2,3,4… | FT 2025 ranks on 27 previously-unranked entries |
| Finance | 1,3,4,7,10… | QS 2026 top 24 complete |
| Marketing | 1,2,3,4,5… | QS 2026 top 15 complete |
| Law | 1,2,3,4,5… | QS 2026 top 13 complete (was entirely unranked) |
| Chemical engineering | 1,2,3,4,7,11,15 | QS 2026 top 15 complete (had 3 entries total) |
| Mechanical engineering | 1,2,3,5,13… | QS 2026 top 20 gaps filled |
| Computer science / AI | 2,3,4,4,10… | See the MIT caveat below |
| Sustainability | 2,2,4,5,6… | QS Environmental Sciences 2026 applied |

### Still outstanding (updated after session 4c)
- **Ranks ~25–50 in most tables.** Still named-and-researched but not added: La Salle Ramon Llull, Purdue and HKUST (supply chain 9–15), JHU Carey, Michigan State, UC Davis, UCSD Rady, Mannheim, ASU, GWU, Texas A&M, Wisconsin, EGADE, Macquarie. Previously listed and now **done**: SKEMA, NEOMA, Grenoble, Leeds, Cranfield, McCombs, UW Foster.
- **Ranks ~20–50 in most tables (original note).** Named, researched, not yet added: SKEMA, NEOMA, Grenoble (marketing/finance entries), Leeds, Cranfield, JHU Carey, Michigan State, UC Davis, UCSD Rady, Mannheim, ASU, GWU, Texas A&M, Wisconsin, EGADE, Macquarie. US MBAs still missing at Emory Goizueta, UNC Kenan-Flagler and Vanderbilt Owen — **their admit rates are already researched** (20%/37%/38%, Class of 2027) but I could not find tuition separated from cost-of-attendance, and the `tuition` field must not carry a COA figure.
- **`SCHOOL_SCHOLARSHIPS` covers 103 of ~340 schools.** Prioritise by best `extRank` — there's a script for that in the session log.
- **`PROGRAMME_ESSENTIALS` is 14 of 551.** Genuinely one programme at a time; the destination-country checklist layer covers everyone else.
- **Public Health has no structured ranking applied** — deliberately. QS publishes no standalone public-health subject table, so the 12 entries keep accurate prose and fall back to curated order. Don't invent one.
- **MIT has no admittable CS master's.** Verified directly: MEng is MIT-undergrad-only, the SM sits inside the PhD track, and the CSE SM has external admissions paused. A "computer science" search legitimately starts at Stanford (#2). Do not "fix" this.

## Session 4 summary (this pass)

### Scholarships — rebuilt as three axes
`GOV_SCHOLARSHIPS` was one entry per country looked up with `.find()`; it is now a flat list filtered by country, so a destination can have several schemes. Three axes now exist and all three render as rows on a programme's funding card:
- **Destination-country** (`GOV_SCHOLARSHIPS`, 30 entries): BE (Master Mind, ARES), FR (Eiffel, BGF), UK (Chevening, GREAT, Commonwealth), DE (DAAD, Deutschlandstipendium, KAS, Böll), IT (MAECI, Invest Your Talent, regional DSU), ES (Carolina, MAEC-AECID), plus NL/CH/SE/IE/AT/EU/US/CA/JP/CN/KR/SG/AU/NZ.
- **Home-country** (`HOME_REGION_FUNDING`, 9 countries): each entry carries `scope` (`abroad`/`domestic`/`both`) plus optional `destCountries`/`destSchools`, so BAEF, Kennedy, Wiener-Anspach and the Fulbright commissions only surface on programmes they can actually fund. Belgium has 6 entries, France 4, UK 4, Spain 4, Italy 3, Germany 4, plus NL/SE/NO/DK portable student finance.
- **Programme-linked** (`GLOBAL_SCHOLARSHIPS`, 11 entries): Erasmus Mundus, Knight-Hennessy, Gates Cambridge, Clarendon, Rhodes, Schwarzman, Rotary Peace, Forté, Weidenfeld-Hoffmann, Skoll, Aga Khan. Matching is data-driven (`schools`/`countries`/`regions`/`fields`, with `requireBoth` and `programMatch` for precision, and `listOnly` for entries that belong in the directory but not on a programme row).
- New **Full funding directory** section on the Scholarships page with axis tabs — replaced three dead render functions (`renderScholarGov`, `renderScholarshipShortlist`, `wireProgrammeGridInteractions`) whose DOM targets had been deleted at some point.

### Search — intent parsing, aliases, relevance
- `parseQuery()` turns free text into structured intent (places / subjects / constraints) and drops filler, so "best programmes in the USA" returns 125 US programmes instead of nothing. Vocabulary is built partly from `PROGRAMS` itself so it can't drift.
- `SEARCH_ALIASES`: ~90 abbreviations (MiM, IB, MSBA, LLM, MPP, ML, LBS, HBS, polimi, uk/usa/holland…).
- **Real bug fixed**: short terms were plain substring matches — `uk` matched **Duke**, `ai` matched **Bahrain** and **Chain**, `us` matched **Business**. Terms ≤3 chars now require a word boundary.
- `queryRelevance()`: title match > school match > field-tag match > blurb match. This is what stops MBAs dominating a "management"/MiM search — they now appear below a labelled "Related programmes" divider.

### Other user-reported fixes
- **Mobile/tablet header**: `.logo` was `display:none` at ≤940px. Restored at every width; only nav + tool group collapse into the hamburger. (Note: session 3's handoff says the user asked for hamburger-only — they have since reversed that. The logo stays.)
- **Belgium post-study visa** was recorded as "no dedicated job-search visa" — wrong. Belgium has a 12-month search year (zoekjaar) under EU Directive 2016/801. Fixed. Austria and Finland added.
- **TUM tuition** was €1,500–3,000 and described as "near-zero public tuition". TUM has charged non-EU/EEA master's students €4,000–6,000 **per semester** since WS 2024/25. All three entries corrected to €8,000/yr with the EU/EEA exemption stated.
- **ETH Zurich MSc Computer Science** had a dead `link` and no `extRank`, so it sorted last in a CS search. Both fixed.

### New: selectivity
`SELECTIVITY` holds published admit rate / applicants / class size, with a `basis` field separating school-published figures from widely-reported third-party ones. 15 programmes covered (the US MBAs, INSEAD MiM cohort size, IMD). Everything else says "not published" and points at the real entry signals rather than inventing a number. Rendered on cards, in the modal and as a compare row.

### New: destination-specific checklist
`DESTINATION_REQUIREMENTS` covers 15 countries with the country-level machinery that actually causes missed intakes — UK CAS + £1,529/£1,171 per month held 28 days + ATAS for technical subjects; Germany uni-assist, APS for India/China/Vietnam, €11,904 blocked account; France compulsory Études en France; Italy compulsory Universitaly pre-enrolment + DoV/CIMEA; Belgium NARIC-Vlaanderen vs FWB equivalence; US I-20/SEVIS/DS-160; Canada's new PAL. Field notes fire only for affected subjects, and the card says plainly when free movement or home citizenship makes the visa section moot. **This applies to all 450 programmes**, unlike `PROGRAMME_ESSENTIALS` (still 14).

### Catalogue: 398 → 450
Belgium 4→25 (KU Leuven ×8, Ghent ×7, UCLouvain ×6 — all programme names and URLs verified by probing the institutions' own URL patterns), plus the world's top MBAs which were **missing entirely** (every QS 2026 Global MBA top-15 school is now present: HEC Paris #5, LBS #6, Cambridge Judge #7, INSEAD #8, IE #11, Oxford Saïd #12, IESE #15, plus Yale SOM, Fuqua, Johnson, Rice, Esade, SDA Bocconi, IMD), plus Western European research universities (LMU, Heidelberg, TU Berlin, KIT, IP Paris, Mines Paris-PSL, PoliMi ×2, PoliTo, UPC, UC3M, UPM, Navarra), plus Oxford MSc Advanced Computer Science.

### Link hygiene
Every URL added or changed this session (113) was checked with a real browser user agent. Ten 404s were found and repointed at live pages. Remaining non-200s are Cloudflare/403 bot blocks (chevening.org, insead.edu, ox.ac.uk, daad.de) that work in a browser.

## Still outstanding after session 4
1. **Catalogue is not yet "fully exhaustive"** — 450 is far short of covering the top-50 global schools across all 24 fields. Biggest remaining gaps: UCL/LSE/KCL/Edinburgh depth (UCL still has 2 entries), the 13 US MBAs at schools already present but without an MBA entry (Stern, Ross, Anderson, McDonough, Tepper, Kenan-Flagler, McCombs, Owen, Foster, Goizueta, Kelley, Fisher, Simon — acceptance rates already researched, tuition not verified), and top-50 schools with no entry at all: Kyoto, Osaka, Tohoku, Lund, Uppsala, Zhejiang, USTC, City University of Hong Kong, UWA, Adelaide, PSL, Paris-Saclay.
2. **`SCHOOL_SCHOLARSHIPS`** is ~58 of 320+ schools. New schools added this session that still need entries: LMU, Heidelberg, TU Berlin, KIT, IP Paris, Mines Paris, PoliMi, PoliTo, UPC, UC3M, UPM, Navarra.
3. **`PROGRAMME_ESSENTIALS`** is still 14 of 450 — real per-programme essay/recommender research, one programme at a time.
4. **MIT has no admittable CS master's.** Verified directly: the MEng is MIT-undergrad-only, the SM exists only inside the PhD track, and the SM in Computational Science and Engineering has external admissions paused. So a "computer science" search legitimately starts at #2 (Stanford). Do not "fix" this by inventing an MIT CS entry. Worth considering a general "why isn't the #1 school here?" note for cases like this.
5. **Subject-ranking sources conflict.** Third-party QS mirrors disagreed on Imperial (#8 vs #12), EPFL (#11 vs #15) and Berkeley (#6 vs #7) for CS 2026. Existing `extRank` values were left alone rather than churned against an unreliable mirror. If rankings are ever refreshed wholesale, use one source consistently.

---

# Campus Atlas — handoff context (session 3, continued from session 2)

## IMPORTANT — deploy state
The repo (`florenvandevelde/campusatlas` on GitHub) had **long stretches of uncommitted local changes during this session** — the user kept reporting fixes as "still broken," which turned out to be because nothing had been pushed to `origin/main` yet, not because the fixes were wrong. Confirmed and pushed in two commits (`56f8d3e`, `0df8612`, `0beba70`) partway through. **Always check `git status`/`git diff --stat` before trusting a live-site bug report in this repo** — a "still broken" report may just mean the fix hasn't been pushed. Someone (the user, most likely, running `push.sh` themselves at points) also committed several "Update site content" commits mid-session outside of this session's own commits — don't assume this session's git log is the complete picture; always diff against current HEAD rather than assuming a clean baseline.

## Session 3 summary (this pass)
- All 52 `SCHOOL_SCHOLARSHIPS` entries now carry a verified official `link` plus real eligibility/process detail (auto-considered vs. separate application, merit vs. need, specific restrictions/deadlines) — was 10/52 at the start of this pass.
- `HOME_REGION_FUNDING` (new): home-country funding, not destination-country. Belgium has 2 real entries — the **Fayat Scholarship** (a Flemish excellence grant for a *second* master's at ~78 QS-top-ranked institutions; the user initially said "Fayette," couldn't be found, then the user supplied the real URL) and the general Vlaamse studietoelage. Both correctly asterisked as Flanders-only, not Belgium-wide.
- Sort-order concern resolved (see prior session entries for the original `prestigeScore` fix): user was worried real QS/FT rankings for India/Africa schools might push them above elite Western schools. Verified live — they correctly sort in their genuinely-earned position (e.g. ISB at position 12, well behind Wharton/Harvard/MIT/Stanford/Kellogg/Haas/Booth/Tuck/Darden) — confirmed via `AskUserQuestion`, user chose "add real rankings, sort strictly by them" over region-based deprioritization.
- Data-quality fix: HEC Paris MiM tuition was stale (€45,000 vs. the actual current €57,700 confirmed on HEC's own fees page) — corrected.
- Catalogue: 392 → 398 programmes. New: Universidad del Pacífico (Peru), Koç University (Turkey), ESCP Marketing, King's College London LLM, Toronto OISE MEd, University of Tokyo Chemistry, Imperial Innovation/Entrepreneurship MSc, HEC Paris Master in Marketing (QS #1 2026) — all tuition/duration/intake figures verified via official pages, not aggregators.

## Latest batch (this pass): catalogue + scholarship depth, mobile header
- **Mobile header reverted to hamburger-only**: user tried logo+hamburger, found the header still didn't fit, asked to go back to *just* the three bars on mobile (Campus Atlas reachable via the dropdown's first item instead). Desktop explicitly confirmed unchanged (logo/Programmes/About + Compare/Checklist/Scholarships). `.logo` hidden again at ≤940px.
- **Catalogue growth (392 → 396)**: ESCP MSc in Marketing and Digital Media (France, QS Marketing #3 2026, €23,900/15mo — official page fetch), King's College London LLM (UK, £38,300/yr confirmed via official kcl.ac.uk fees page), University of Toronto OISE MEd (Canada, CAD $48,150/yr international confirmed via official oise.utoronto.ca fees page, 18mo), The University of Tokyo Master's in Chemistry (Japan, new field-diversifying entry, standard low national-university tuition matching the existing UTokyo CS entry's convention). All verified via official pages or corroborated search, node syntax + dupe-checked after.
- **Scholarship base depth (user-requested, "keep refining")**: added real `link` fields + rewrote `desc` with actual verified eligibility/process detail (auto-considered vs. separate application, need vs. merit basis, renewal conditions, typical award ranges) for INSEAD, London Business School, Wharton (corrected — Joseph Wharton Fellowship is genuinely merit-based/automatic, not need-based as previously described), Harvard, Stanford GSB, Columbia, Chicago Booth, UC Berkeley Haas, Dartmouth Tuck (new SCHOOL_SCHOLARSHIPS entry), UVA Darden (new SCHOOL_SCHOLARSHIPS entry, incl. Jefferson/Oculus named fellowships). Wired the real `link` into both the funding modal's "More information on that scholarship" button and `getProgrammeScholarshipData` — falls back to the scoped Google search only when no verified link exists yet. ~40 of ~52 SCHOOL_SCHOLARSHIPS entries still only have the older, shallower `{name, desc}` shape without a verified link — this is real, ongoing per-school research, not a quick pass.

## What changed this session
- **Shortlist checkbox desync bug (user-reported, fixed)**: adding a programme to the shortlist via the card's "Scholarships" tag (`showPage('scholarships', null, p.id)`) updated `state.compare` and the badge count correctly, but the Programmes-page grid was never re-rendered, so its checkbox DOM went stale. Clicking the (visually unchecked, but actually-selected) checkbox then toggled it the wrong way — user had to click twice to get back to a consistent state. Fixed by calling `render()` whenever `showPage("programmes")` runs (`index.html` inside `showPage()`), so the grid — and its checkboxes — always reflect `state.compare` on arrival. Verified via direct DOM/state inspection in the browser (add via Scholarships shortcut → navigate back → checkbox now correctly checked and toggles cleanly in one click).
- **Mobile/tablet header + filters redesign (user-requested)**: at ≤940px the full header (logo, Programmes/About nav, Compare/Checklist/Scholarships tool group, shortlist badge) now collapses to a hamburger button (`#navToggle`) — logo/brand stays visible and clickable (initially over-collapsed to hamburger-only per a literal reading of the first request, then corrected after user feedback: logo must stay). Opening it shows a dropdown panel (`#mobileMenu`) listing Campus Atlas (home)/Programmes/About Campus Atlas, then Compare/Application Checklist/Scholarships — all three now carry the same shortlist-count badge (`mmCompareCount`/`mmChecklistCount`/`mmScholarshipsCount`) under a "Your shortlist — N applies below" label, so it's clear the count applies to all three, not just Compare. The filters sidebar is a collapsible panel at ≤940px (tablet and mobile), closed by default. Stats tiles: dropped to 3 (Months hidden) at ≤620px so font stays readable on one line; tightened `.stats`/`.board` spacing so Filters doesn't sit far below. Search placeholder is now viewport-width-aware (`updateSearchPlaceholder()`) so it doesn't get cut off mid-word ("...no GMAT"... becoming a stray "n") on narrow screens.
  - **Real bug found and fixed**: the mobile-menu backdrop (`z-index:65`, sibling of `<header>`) outranked the *whole header* (`z-index:60`) at the page-stacking level, even though the menu inside the header declared `z-index:70` — a child's z-index only competes within its own ancestor's stacking context, so the header (capped at 60) rendered *under* the backdrop entirely. This is exactly what the user saw as "the page just becomes very dark" when opening the hamburger. Fixed by dropping the backdrop to `z-index:55`, below the header's own stacking level.
  - **Real bug found and fixed**: `.checklist-card-remove` on the Application Checklist page used `position:absolute; top/right`, so once the programme title wrapped to 2–3 lines on a narrow screen the Remove button just floated on top of the wrapped text. Fixed by moving it into a normal flex toolbar row (`.checklist-card-toolbar`) above the card content — can't overlap at any width now.
  - **Real bug found and fixed**: two mobile horizontal-overflow sources on the Scholarships/Checklist pages — `.pill-amount` (funding-amount badge) had `white-space:nowrap` despite holding full-sentence text like "€1,200/month living allowance (2026); tuition is not generally covered", and `.pfield select` had no `width:100%` so native `<select>` sized itself to its longest `<option>` text (e.g. the full country list), both silently forcing `document.documentElement.scrollWidth` well past the viewport. Fixed both; verified `scrollWidth === innerWidth` at 375px on both pages afterward.
- **GMAT/GRE and TOEFL/IELTS test-centre locator — real bug found and fixed**: `findNearbyCentres()` searched Nominatim for a bare chain name ("Pearson VUE", "Prometric") with **no geographic bias at all**, so it returned Nominatim's global "importance" sample (verified via direct `curl`: a bare search returns scattered hits in the UK/Romania/US/India/UAE regardless of query origin) — for the vast majority of real searches, none of that arbitrary global sample fell within `MAX_CENTRE_KM`, so the locator came back empty ("it's just not showing anything", exactly as reported). Fixed by adding Nominatim's `viewbox`+`bounded=1` params (`bboxAround()`), confirmed via `curl` that this correctly narrows results to genuinely nearby matches when OSM has them. Separately, per the user's ask for actual "dots on a map," replaced the old single-marker `openstreetmap.org/export/embed.html` iframe (which only ever supported one marker) with a real Leaflet map (`leaflet@1.9.4` via unpkg CDN, confirmed CORS-reachable from `file://` with a real browser UA) rendering a black dot for the searched location plus one accent-coloured dot per nearby centre found, auto-fit to bounds, each with a popup. Verified end-to-end for "Brussels, Belgium" → 2 real Pearson VUE UK branches plotted at correct distances (295km, 345km) matching the list below the map.
- **Sort-order bug (user-reported, fixed)**: `prestigeScore()` parsed *any* "#N" or "Top N" pattern out of the free-text `extRank` field regardless of source, so a regional claim like GIBS's `"Frequently Africa's #1 MBA (FT)"` scored identically (1) to Wharton's genuinely-global `"QS Global MBA #1 (2026)"`, pulling Africa's best-regional MBA to the top of a global "mba" search. Fixed by requiring the *structured* format this catalogue actually uses for real global QS/FT numbers — `/^(QS|FT)\s.*\(\d{4}\)\s*$/` — before trusting any extracted number; verified all 127 genuine QS/FT-ranked entries match that structure and all 13 narrative/regional "#N" claims (Canada's #3, Africa's #1, US News #1, etc.) now correctly fall back to curated order instead. The descriptive `extRank` text itself is untouched — still shown as a badge, just no longer drives sort order. Verified live: searching "mba" now returns Wharton/Harvard/Columbia/MIT/Stanford first; GIBS/Stellenbosch now sort last (22nd/23rd of 23).
- **Catalogue growth (390 → 392)**: added Universidad del Pacífico — Pacífico Business School MBA (Lima, Peru — new country) and Koç University Graduate School of Business MBA (Istanbul, Turkey — new country, bucketed under `region:"Asia-Pacific"` to match this catalogue's existing convention for Middle-East-adjacent countries like Lebanon/Saudi/UAE, since there's no separate Middle East region). Both fully verified: Peru's tuition/intake/GMAT-policy pulled directly from the official `pbs.edu.pe` admissions page (87,500 PEN, August 2026 start, no GMAT — uses the school's own aptitude test); Turkey's from search-corroborated official figures (750,000 TRY, 12-month, September intake, GMAT/GRE required, avg. GMAT ~560). Added `PEN`/`TRY` to `CURRENCY_BY_COUNTRY`. **Explicitly skipped this round despite research time spent** (per the no-fabrication rule — could not get clean/confident tuition, deadline, or GMAT-policy facts from official sources): Rabat Business School (Morocco — official site had an SSL cert error via fetch, secondary sources gave a wide 72k–110k MAD/year range with no single confirmed figure), Sasin School of Management (Thailand — official page states a genuinely flexible 12–48 month duration with no single "standard" number, and intake/GMAT policy unconfirmed), Asian Institute of Management (Philippines — only found a fee for their distance-learning Online MBA, not the full-time on-campus programme). Worth a dedicated follow-up pass if the user wants these specifically.

- **PDF download for the Application Checklist (built)**: "Download PDF" button next to Remove on each checklist card, next to the existing `.checklist-card-toolbar`. `downloadChecklistPDF(progId)` clones the *already-rendered* card (so the PDF always matches what's on screen — no separate content to keep in sync), force-opens every `<details>` section, strips controls that mean nothing on paper (search input, buttons, the Leaflet map), injects it into a hidden `#printArea`, and calls `window.print()` — the browser's own "Save as PDF" is the actual download, no library/backend. Verified end-to-end with `window.print` stubbed: all 6 categories present, all `<details>` open, toolbar/locator/inputs/buttons correctly stripped, `document.title` set to the programme name for a sensible default PDF filename and restored via `afterprint`.
- **Global mobile side-scroll/"wobble" (user-reported, fixed)**: added `html, body { max-width:100%; overflow-x:hidden; overscroll-behavior-x:none; }` as a defensive guard — verified via a DOM scan (`getBoundingClientRect().right > viewport`) across Programmes/About/Checklist/Scholarships/the Compare overlay/the mobile menu/the programme-detail modal at 375px that nothing actually needs to overflow; the only legitimate horizontal scroll left is the Compare table's own contained `overflow-x:auto` (a wide data grid, expected).
- **Test-centre-locator resource links overhauled (user-reported, fixed)**: the "Open GMAT/GRE/TOEFL/IELTS search in Maps" buttons were a Google Maps *text-search guess* — user reported it returning UK/France/Germany results for a search near Ninove, Belgium, despite having personally sat the test in Mechelen/Brussels. Replaced with links to each programme's actual **official, always-current locator tool** (verified reachable via `curl`): Pearson VUE's test-center locator for GMAT, ETS's `maprequest.ets.org` for GRE, ETS's TOEFL schedule page, `ielts.org/test-centres` for IELTS. Also fixed the "GMAT Official Prep" link (was bare `mba.com` homepage → now the actual prepare-for-the-exam page) and the GRE equivalent, and removed the "Search GMAT Club for [school] threads" link per explicit feedback ("that is terrible, take it out").
- **Checklist card cleanup (user-reported)**: removed category "06 Community & insight resources" (Clear Admit/GMAT Club/Reddit/YouTube search links) as its own tab — user's point: checklist tabs should only be things you check off to get your application ready, not a discussion-link dump. The one genuinely valuable, already-verified piece (a real per-school Clear Admit link from `SCHOOL_CLEARADMIT`) was folded into the "Statement of purpose / essays" section instead, alongside a trimmed, higher-quality essay-resource list (mbaMission + Poets&Quants, the most-read source in this space) — dropped MBA Crystal Ball/GMAT Club Blog/Reddit per "only include relevant examples... otherwise you're wasting everybody's time." `communityLinksHTML()` removed as dead code.
- **Scholarship-modal "apply" link (user-reported, fixed)**: the school merit-award modal's button said "Apply via Official [School] Portal" but linked to the programme's *general* application page, not anything scholarship-specific — misleading, since most school merit awards in this catalogue are "applied for automatically after admission," not a separate application. Relabelled to "More information on that scholarship ↗" and pointed at a Google search scoped to the specific award name + school (matching this codebase's established pattern for "can't verify one exact URL, so link a scoped live search instead of guessing").
- **Home-region scholarships (new, user-requested)**: added `HOME_REGION_FUNDING`, a new data axis alongside `GOV_SCHOLARSHIPS` — funding tied to *where the applicant is from* (usable for study anywhere abroad) rather than the destination country. `HOME_REGION_FUNDING[country]` is an array (multiple real schemes can apply to one country); renders as "🏠" funding-option-rows on the Scholarships page whenever `state.profile.citizenship` is set and differs from the programme's country. Belgium has two verified entries: **Fayat Scholarship (Fayatbeurzen)** — the one the user actually meant (initially heard as "Fayette"; user later supplied the real URL, `vlaanderen.be/en/studying/fayat-scholarship`) — a Flemish excellence grant specifically for a *second* master's at one of ~78 QS-top-5-per-field institutions across 13 countries, real costs reimbursed up to 24 months, deadline 1 Dec, requires C1 Dutch + a master's already obtained from a Flemish institution (not just Belgian nationality — verified via `ugent.be`'s student-facing summary page); and the more general **Vlaamse studietoelage** (means-tested, conditionally usable for a first master's abroad) as a secondary entry. Both clearly asterisked as Flanders-region-specific, not nationwide (Wallonia/French-speaking Brussels use a separate Fédération Wallonie-Bruxelles scheme). **Scope note**: user wants this "extensive" across "top countries" — only Belgium is done; expanding to other countries is a real per-country research effort, not started yet.
- **Mobile stats polish (user-reported, fixed)**: the 3 stat tiles were left-aligned within their own flex slot rather than visually centered as a group, and there was more gap than needed before Filters. Added `text-align:center` to `.stat` at ≤620px and tightened `.hero`/`.board` padding further.
- **Programme-card layout bug (user-reported, fixed)**: `.card-select` ("Add to compare"/"Selected for compare") was a *normal-flow* flex child (not absolutely positioned, unlike the "TOP N" rank ribbon) stacked **above** the flag/school row in the card's own `flex-direction:column`, right-aligned via `align-self:flex-end` — so on every card without a rank badge, the compare toggle sat alone on its own row with the rest of that row empty, and the school name started on a visually disconnected row below it. Restructured `cardHTML()` so flag+school and the compare toggle are now siblings inside a new `.card-head` row (`display:flex; justify-content:space-between`), so they share one horizontal line — with `flex-wrap:wrap` as a graceful fallback on the narrowest single-column mobile cards, where they still stack but with a normal small gap instead of a large disconnected one. Verified at both 1280px (one line) and 375px (wraps cleanly, no overlap) widths.

## Still queued (asked for, not yet started)
- **Extensive home-region scholarship research beyond Belgium** — user wants this "really extensive," scouted per top country in the catalogue, each with the same region-dependency honesty/asterisk treatment where applicable. Needs a dedicated research pass per country, verified through official sources — no guessing at scholarship names or amounts.

Static prototype in [index.html](index.html) — single-file HTML/CSS/JS, no backend, no build step. Helps bachelor's/master's students discover, shortlist, compare, and prepare applications for prestigious global business/STEM master's programmes.

## Current state
- **392 programmes** in `PROGRAMS` (was 390 at start of this session, 217 the session before, 184 before that), spanning 46 countries, all 5 regions.
- Every addition/edit this session was verified with a Node syntax check (`new Function(source)` over the extracted `<script>` block) and a live browser reload before moving on — do the same after any further edit.
- No duplicate `id`s, no duplicate school+program pairs (both scripted and checked at end of session).

## What changed this session

### Bug fixes (user-reported, all verified fixed)
- **Test-centre locator was showing wrong/random locations** (e.g. Toronto for a Belgium search). Root cause: it used a keyless Google Maps embed (`maps.google.com/maps?q=...&output=embed`), which turned out to be unreliable for arbitrary queries. Replaced with: (1) an accurate single-marker OpenStreetMap preview map via Nominatim geocoding (confirmed to work from `file://` — Overpass API does *not*, it blocks `Origin: null` requests with a 403/Cloudflare challenge, confirmed by directly testing), (2) a real named-venue lookup (Pearson VUE/Prometric for GMAT/GRE, British Council/IDP/Prometric for TOEFL/IELTS) via Nominatim free-text search + haversine distance filtering (max 350km), rendered as a single-column list with honest distances, (3) an honest "not found in our directory" fallback plus the official Google Maps Search URL (`google.com/maps/search/?api=1&query=...` — the documented API, reliable) when no real venue is found nearby.
- **GMAT Club link mislabeled** — "Discuss X on GMAT Club" implied a direct link but was actually a Google search scoped to `site:gmatclub.com` (GMAT Club's own search requires login, confirmed via direct curl — got a Cloudflare challenge). Relabeled to be honest about what it does.
- **Duolingo shown for schools that don't accept it** — the "Preparation resources" list in the English-test checklist card showed a Duolingo link unconditionally regardless of the app's own region-based `getEnglishTests()` logic. Now gated so the resource list matches the "Recorded accepted tests" line above it.
- **Hero eyebrow / stat tiles could drift from real data** — "176 programmes" etc. were hardcoded strings, not computed from `PROGRAMS`. Now all four homepage stat tiles (`statPrograms`, `statCountries`, `statTuitionRange`, `statMonthsRange`) and the hero eyebrow text are computed live from `PROGRAMS`.
- **Shortlist badge felt disconnected** from the Compare/Checklist/Scholarships segmented control — moved it inside `.header-tool-group` as a docked last segment instead of a separately-spaced element.
- **Scholarships tab required manual re-selection** — clicking "Scholarships" on a specific programme card just opened the generic Scholarships page with nothing selected. `showPage()` now accepts an optional `progId` third argument; when provided it adds the programme to the shortlist, pre-selects it in the target-programme dropdown, and renders its funding breakdown immediately. Both call sites (card tag and modal button) now pass `p.id`.

### New features
- **Real per-programme application research** (`PROGRAMME_ESSENTIALS` object, keyed by programme `id`): actual current essay prompts/word counts and recommender counts/sources, sourced via live WebSearch against official pages and reputable admissions-consulting coverage (Clear Admit, mbaMission, Stacy Blackman etc.). Currently covers 14 programmes — INSEAD MiM, LBS MiM, HEC Paris MiM, Wharton/Harvard/MIT Sloan/Stanford/Columbia/Kellogg/Booth/Haas/Tuck/Darden MBAs, and ISB PGP. **Important**: several near-misses were deliberately *not* added because the researched programme (usually an MBA) didn't match the catalogued degree (usually a MiM) at that school — e.g. Yale, Duke Fuqua, IESE, IE were researched but skipped for this reason. Always check `program:` matches before adding a new entry to this table.
- **Verified GMAT/GRE class averages** (`GMAT_AVG` object): 12 programmes with real, sourced class-average scores (legacy + GMAT Focus scale), all other `gmat:true` programmes show an honest "not verified — check official class profile" fallback.
- **Named school scholarships** (`SCHOOL_SCHOLARSHIPS` object): ~50 schools with real named merit/need scholarship programmes (e.g. HBS's need-only policy, Wharton Fellowships, Cornell's Park Leadership Fellows) instead of generic boilerplate. Unmatched schools keep the honest generic fallback.
- **Community/insight resources**: every checklist card has a "06 Community & insight resources" box with a confirmed-real Clear Admit link for 12 schools (`SCHOOL_CLEARADMIT`) plus live search links (GMAT Club, Reddit r/MBA + r/gradadmissions, YouTube) scoped to the school/programme name — never a claimed specific video/post, since that can't be verified to still exist.
- **SOP/essay guidance**: length/structure framework (word counts, what to cover) plus real, verified resources (mbaMission, MBA Crystal Ball, GMAT Club Blog, r/MBA, r/gradadmissions).
- **Recommendation-letter guidance**: a general 4-point structure outline (how they know you, specific evidenced examples, peer comparison, closing statement of support), plus real per-programme recommender counts where researched.
- **Rolling-deadline granularity**: `applicationTiming()` now estimates a "rough last-call" window (~7 weeks before intake) for Rolling-deadline programmes instead of just "check availability now" — clearly labelled as an estimate, not an official date.
- **Local-currency tuition display**: new `tuitionDisplay(p)` function + `CURRENCY_BY_COUNTRY` map. Non-Eurozone programmes now show `US$189,000 (~€175,000)` style dual display (local currency first, approximate EUR conversion muted/smaller alongside); Eurozone countries just show euros (no redundant conversion). The underlying `p.tuition` number is unchanged — it's still the single EUR reference figure, `tuitionDisplay` just converts it for display at an approximate, clearly-labelled rate. Wired into: programme cards, compare grid, checklist card header, scholarship breakdown card, funding modal, programme detail modal. The tuition *filter slider* and the aggregate `statTuitionRange` tile deliberately stay EUR-only (they span all countries, a single common currency is more useful there).
- **Finer work-experience brackets**: `pWork` select now has 5 options (None yet / Up to 2 years / 2–3 years / 3–5 years / 5+ years) instead of 3, matching how real programmes bucket applicants. `eligibility()`'s hard-fail logic is unchanged (only triggers on 0-years-vs-programme-requires-experience) — the extra granularity is honest self-identification, not a new hard filter, consistent with this project's established "soft caution over hard exclusion" philosophy.
- **New FIELDS categories**: added AI, Energy, Public Policy, Public Health, Earth Sciences, Chemistry, Information Science, Education to the filterable field-tag list (was just Management/Finance/Analytics/.../Social Sciences).
- **"Popular" quick-filter chips**: replaced Physics and History (low-value, and "physics" as a bare query had zero false-positive risk but "history" and "physics" weren't the most requested categories) with Sustainability and "Data Science & AI" — verified these produce clean, relevant result sets (tested via `matches` word-substring logic before committing to the query text, since bare 2-letter tags like "AI" would false-positive match unrelated words like "Management"/"Analytics"/"Bahrain" as substrings).

### Catalogue growth (this session: 217 → 390)
Added ~173 real programmes across previously-thin categories and regions: AI/CS (Edinburgh, Cambridge, CMU, ETH, NUS, Georgia Tech, USC, Purdue, Maryland, Johns Hopkins, Waterloo, Melbourne, Trinity Dublin, Wisconsin, UT Austin, Peking, Tsinghua, Fudan, KAUST, Ashesi, NTU Taiwan), Chemical/Civil/Mechanical/Electrical/Aerospace Engineering (Imperial, MIT, Delft, ETH, Berkeley, Michigan, Cranfield, Politecnico di Torino/Milano, Strathclyde, CU Boulder, Texas A&M, Case Western), Energy/Sustainability (Imperial, ETH, Delft, NTNU, Columbia, Yale, Cambridge, KTH, Alberta, Calgary, Khalifa), Public Policy (Harvard Kennedy, Oxford Blavatnik, Georgetown, NUS, Columbia SIPA, Princeton SPIA, Chicago Harris, Wisconsin La Follette, Sciences Po, Cambridge), Public Health (Harvard Chan, Johns Hopkins Bloomberg, LSHTM, Imperial, Karolinska, Toronto, Emory, UW, Yale, Cape Town), Earth Sciences/Physics/Chemistry (Caltech, MIT, ETH, Imperial, Oxford, Cambridge, Colorado School of Mines, UT Austin, Wits), Law/LLM (Harvard, Yale, Oxford BCL, Columbia, NYU, LSE, NUS, Melbourne, HKU), Information Science (Cornell, Syracuse, Toronto), Education (Columbia TC, Penn GSE, Melbourne, Vanderbilt Peabody, USC Rossier), plus significant regional diversification (Latin America: Argentina, Mexico, Chile, Brazil; Africa: Ghana, Kenya, Nigeria, South Africa x4; Middle East: Saudi Arabia, UAE, Israel, Lebanon; Central/Eastern Europe: Czech Republic, Austria, Hungary; more Nordics: Norway, Finland, Denmark). Also added the schools from a "Business School Fair" flyer the user shared that weren't yet covered: Hult, Solvay Brussels, Luiss, Maastricht School of Management, EADA, College of Europe.

## Explicitly out of scope / can't be done as literally requested
- **"Always synced with the source websites"**: not achievable. This is a static file with no backend/scraper, and browsers block cross-origin fetches to arbitrary sites without CORS permission — confirmed directly (a plain fetch to GMAT Club got Cloudflare-blocked even from a real page load). What's deliverable and being done instead: one-time real research per programme (`PROGRAMME_ESSENTIALS`, `GMAT_AVG`, `SCHOOL_SCHOLARSHIPS`) that becomes static data, always hedged as "verify on the official portal — this changes every cycle." Communicated this directly to the user; they accepted it and asked to keep expanding the researched set.
- **"Pull a specific, individually-verified document checklist from that school's own site for literally all 390 programmes"**: not realistic without ~390 individual research passes (huge time cost, and many school portals are login-gated or JS-rendered). Approach instead: keep growing `PROGRAMME_ESSENTIALS` incrementally with real verified research, general (non-fabricated) guidance for the rest.
- **"Top 50 per category" across 14 categories (~700 total)**: flagged as unrealistic at full quality in one pass — some sub-disciplines (e.g. a standalone "Civil Engineering masters" brand) don't have 50 globally-distinct prestigious dedicated programmes without diluting the "prestigious only" bar. Made substantial real progress (see catalogue growth above) rather than padding with lower-quality entries.

## In-flight / not yet done (from the most recent messages, session ended before these were completed)
1. **Regional/sub-national scholarships** — user asked to add regional scholarships (e.g. Flanders-specific funding within Belgium, mentioned a name that sounded like "fayat" — could not identify a real scholarship by that name; needs a proper WebSearch pass to find the real Flemish/regional scheme before adding anything, per this project's strict no-fabrication rule). Needs to be added to `GOV_SCHOLARSHIPS` or a new regional-scholarships structure, clearly asterisked/caveated that eligibility depends on the applicant's specific region within the country (not just national citizenship).
2. **"Spend time on scholarship options, give ranges/coverage amounts, concrete process, official links"** — user wants `GOV_SCHOLARSHIPS` and `SCHOOL_SCHOLARSHIPS` expanded with real, researched amount ranges and clearer process descriptions. Some entries already have real figures (Eiffel: €1,200/month; DAAD: €992/month); this needs a systematic WebSearch pass per scholarship to verify current amounts before adding more specificity — do not invent figures for the ones that currently say "amount varies."

## Established conventions to keep following
- Never fabricate: no invented URLs, video titles, specific scholarship amounts, or admissions-committee "insider knowledge." Always prefer a live search link over a guessed specific page if the real one can't be verified to exist.
- Every new programme entry: check the school isn't already in `PROGRAMS` with the same programme name (`grep -oP 'school:"..."'`) before adding; verify with a Node syntax check + id/dupe count after every batch; then a live browser reload to visually confirm before moving to the next batch.
- Match `PROGRAMME_ESSENTIALS`/`GMAT_AVG`/`SCHOOL_SCHOLARSHIPS` entries to the *exact* catalogued degree type (MBA vs MiM vs MSc) — several near-misses were caught and correctly skipped this session.
